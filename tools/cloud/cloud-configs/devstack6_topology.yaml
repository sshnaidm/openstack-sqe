servers:
  devstack-server:
    params:
      hostname:
      count: 1
      storage: 60
      add_disks:
      ram: 8
      cpu: 1
      networks:
#        - boot
        - admin
networks:
#  - boot:
#      dhcp: true
#      external: false
#      nat: true
#      dns:
#        devstack: 2
  - admin:
      dhcp: true
      external: true
      nat: false 
      dns:
        devstack: 2
      ipv6: true
